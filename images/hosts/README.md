# /images/hosts

Optional. Drop host photos here (square images work best, at least
400×400px) and set the `photo` field in `data/hosts.js` to the path,
e.g. `images/hosts/dope-as-yola.jpg`.

Leave `photo: ""` and the site automatically shows a gradient initials
badge instead — no broken image icons.
